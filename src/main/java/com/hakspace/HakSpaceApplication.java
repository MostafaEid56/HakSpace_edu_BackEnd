package com.hakspace;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class HakSpaceApplication {
    public static void main(String[] args) { SpringApplication.run(HakSpaceApplication.class, args); }
}